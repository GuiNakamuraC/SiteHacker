// Função para redirecionar para inicial.html
function goToInitialPage() {
    window.location.href = 'inicial.html';  // Redireciona para a página inicial
}

// Função para redirecionar para senha.html
function goToSenhaPage() {
    window.location.href = 'senha.html';  // Redireciona para a página de senha
}

// Função para exibir uma mensagem
function showMessage(message) {
    // Cria um alerta personalizado
    alert(message);
}